/**
 * The Soroban Authorization Entry Inspector is fully offline.
 *
 * It decodes authorization entries from a pasted transaction envelope locally,
 * so no network mocks are required.
 */
export const handlers: never[] = [];
